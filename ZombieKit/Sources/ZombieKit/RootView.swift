import SwiftUI

public struct RootView: View {

  public init() {}
  
  public var body: some View {
    List {
      Text("TODO: List of games you've played, ordered by date and a button to create a new one")
      Text("TODO: And we should just create a new game if you haven't played any before, I imagine")
    }
  }
}
